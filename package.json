{
  "name": "groundification",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "./dist"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=test-app",
    "build": "npm run build --workspace=test-app",
    "build:package": "tsc --project tsconfig.build.json && cp src/components/LiquidBackground.module.css dist/components/LiquidBackground.module.css",
    "start": "npm run start --workspace=test-app",
    "lint": "npm run lint --workspace=test-app"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19"
  }
}
